name: test failure (delete me)

# Throwaway workflow to confirm GitHub sends failure emails.
# Manual-only, no secrets, no Telegram, touches nothing else. Delete after use.

on:
  workflow_dispatch: {}

jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - name: Fail on purpose
        run: |
          echo "Deliberate failure to test GitHub failure notifications."
          exit 1
